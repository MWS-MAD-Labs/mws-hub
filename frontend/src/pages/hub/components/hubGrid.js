// Column count follows a comfortable tile width (~260-320px), not a fixed
// number: two on a phone would squeeze the name to a few characters, and the
// wide desktop breakpoints exist so the grid uses the viewport instead of
// leaving a dead right-hand column.
export const HUB_GRID_CLASS =
    "grid grid-cols-1 gap-2.5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5";
